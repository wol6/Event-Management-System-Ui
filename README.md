# intall packages
npm i

# to start project
npm run dev

# Admin Access
Admin functionality is currently enabled by manually updating the user record in the database.

Set the isAdmin field to true in user collection:

Note: Email-based admin approval/setup has not been implemented yet, so admin privileges must be assigned directly through the database.
